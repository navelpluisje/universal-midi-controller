// import { useEffect } from 'react';
// import { useAppDispatch } from 'mainWindow/store';

export const useMidiConnection = () => {
  // const dispatch = useAppDispatch();

  // // const addMessage = (_: unknown, message: MidiMessage) => {
  // //   dispatch(midiActions.addMidiMessage(message));
  // // };

  // useEffect(() => {
  //   window.midiAPI.onMidiMessage(addMessage);
  // }, []);
};
